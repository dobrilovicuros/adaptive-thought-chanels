# adaptive-thought-chanels
A Von Neumann-inspired neural network that learns to interpret logic from truth tables, dynamically prunes up to 80% of its neurons, and autonomously allocates computing resources.
